import React from 'react'
import { Text, StyleSheet } from 'react-native'

export default function TabTwoScreen() {
  return <Text style={styles.title}>TRANG TÀI KHOẢN</Text>
}

const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
  },
})
